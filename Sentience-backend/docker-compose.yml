version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sentience
      POSTGRES_PASSWORD: sentience
      POSTGRES_DB: sentience
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
